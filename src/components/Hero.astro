---
import FaultyTerminalBackground from './FaultyTerminalBackground.tsx';
---

<section class="relative overflow-hidden rounded-3xl border border-slate-200 bg-gradient-to-br from-slate-50 via-white to-white p-6 sm:p-10">
  <div class="relative z-10">
    <div class="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white/70 px-3 py-1 text-xs text-slate-600">
      <span class="h-2 w-2 rounded-full bg-emerald-500"></span>
      <span>欢迎来到我的个人主页</span>
    </div>

    <h1 class="mt-5 text-3xl font-semibold tracking-tight sm:text-5xl">
      这里是你的姓名 / 职位
    </h1>

    <p class="mt-4 max-w-2xl text-sm leading-7 text-slate-700 sm:text-base">
      写一句你擅长的方向与价值：比如“打造沉浸式交互体验 / 把想法落地成产品”。
    </p>

    <div class="mt-7 flex flex-wrap gap-3">
      <a
        href="/portfolio"
        class="inline-flex items-center rounded-full bg-slate-900 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800"
      >
        进入作品集
      </a>
      <a
        href="/about"
        class="inline-flex items-center rounded-full border border-slate-300 bg-white/70 px-4 py-2 text-sm font-medium text-slate-900 hover:bg-white"
      >
        了解我
      </a>
    </div>
  </div>

  <FaultyTerminalBackground
    client:only="react"
    className="pointer-events-none absolute inset-0 z-0"
    scale={1.2}
    gridMul={[2, 1]}
    digitSize={1.5}
    timeScale={0.3}
    scanlineIntensity={0.35}
    glitchAmount={1}
    flickerAmount={1}
    noiseAmp={1}
    chromaticAberration={0}
    dither={0.5}
    curvature={0.25}
    tint="#8b5cf6"
    mouseReact={true}
    mouseStrength={0.25}
    pageLoadAnimation={true}
    brightness={1}
  />
</section>
